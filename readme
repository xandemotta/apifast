
pipenv install
pipenv shell

docker build -t frontend-image .
docker run -it -p 8080:8081 frontend-image


docker build -t backend-image .
docker run -it -p 8000:8000 backend-image

uvicorn app.main:app --reload --host 0.0.0.0 --port 8000